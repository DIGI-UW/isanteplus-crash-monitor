# isanteplus-monitor configuration

# ── HTTP health check ────────────────────────────────────────────────
# URL to check for responsiveness
SESSION_URL="http://localhost:8080/openmrs/ws/rest/v1/session"

# Timeout in seconds for the HTTP request
TIMEOUT=15

# Polling interval in seconds
POLL_INTERVAL=15

# ── Paths ────────────────────────────────────────────────────────────
# Directory to store diagnostic output
OUTPUT_DIR="/var/log/isanteplus-monitor/"

# Path to the OpenMRS log file
OPENMRS_LOG="/usr/share/tomcat7/.OpenMRS/openmrs.log"

# ── Tomcat ───────────────────────────────────────────────────────────
# Whether to restart Tomcat7 after collecting diagnostics
RESTART_TOMCAT=true

# Pattern to find the Tomcat JVM process (used by all scripts)
TOMCAT_PATTERN="catalina.startup.Bootstrap"

# Max time in seconds to wait for Tomcat to come back after a restart.
# Also used as the post-restart cooldown: health checks are skipped for this
# duration after any restart (monitor-triggered or external) to let OpenMRS
# finish initializing.
RESTART_TIMEOUT=300

# Grace period in seconds after system boot before health checks begin.
# OpenMRS module loading can take 10+ minutes on first start; this prevents
# the monitor from collecting diagnostics or restarting Tomcat while that
# initial startup is still in progress. Set to 0 to disable.
BOOT_GRACE_PERIOD=900

# ── MySQL ────────────────────────────────────────────────────────────
MYSQL_USER="root"
MYSQL_PASSWORD=""
MYSQL_DATABASE="openmrs"
MYSQL_HOST="localhost"

# ── Snapshots ────────────────────────────────────────────────────────
# How often (in minutes) the snapshot cron job runs.
# Applied on install/reinstall — changes require re-running install.sh.
SNAPSHOT_INTERVAL=5

# ── Retention ────────────────────────────────────────────────────────
# Days to keep incident diagnostics and snapshot data
RETENTION_DAYS=7

# ── Heap dumps ───────────────────────────────────────────────────────
# Full heap dumps (jmap -dump) produce 1-2GB files.
# Disabled by default; jstack thread dumps and jmap -histo are always captured.
ENABLE_HEAP_DUMP=false

# ── JDK tools ────────────────────────────────────────────────────────
# If jstat/jstack/jmap are not on the default PATH, set JAVA_HOME here.
# The scripts will add $JAVA_HOME/bin to PATH if set.
# Example: JAVA_HOME="/usr/lib/jvm/java-8-openjdk-amd64"
#JAVA_HOME=""
